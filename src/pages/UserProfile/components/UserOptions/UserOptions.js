import myCompile from '../../../../utils/myCompile';
import template from './UserOptions.tmpl';
import './UserOptions.scss';

const UserOptions = (props) => myCompile({ template, props });

export default UserOptions;

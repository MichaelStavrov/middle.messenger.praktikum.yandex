import myCompile from '../../../../utils/myCompile';
import template from './UserAvatar.tmpl';
import './UserAvatar.scss';

const UserAvatar = (props) => myCompile({ template, props });

export default UserAvatar;
